/** De dónde llega el texto: escrito a mano o subido como archivo. */
export type Source = 'write' | 'upload';

/** En qué momento del recorrido está la pantalla. */
export type Stage = 'compose' | 'result';
