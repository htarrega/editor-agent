# The corpus is the author's own prose and is deliberately outside the image,
# the same way it is outside the repository (docs/PLAN.md, H0).
evals/corpus/
evals/results/

.git/
.github/
.env
venv/
__pycache__/
*.py[cod]
*.egg-info/
.ruff_cache/

# Not part of what runs.
tests/
docs/
